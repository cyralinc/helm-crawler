controlPlane: "stable.dev.cyral.com"

credentials:
  existingSecret: "my_existing_secret"

cronjob:
  repoName: "repo_name"
  repoCredentials:
    existingSecret: "my_existing_secret"
  schedule: "0 0 * * 6"
  serviceAccount:
    name: sa-user
    create: false
